# Dependencies
node_modules/

# Env and secrets (never commit real keys)
.env
.env.local
.env.*.local
admin/.env.local
# js/shahmaran/config.js – keep stub (empty) committed; don’t commit real keys

# Next.js (admin)
admin/.next/
admin/out/

# Flutter / Dart
mobile/.dart_tool/
mobile/.packages
mobile/build/
mobile/*.iml
mobile/.idea/
mobile/.metadata

# Supabase local (if you use CLI)
supabase/.branches
supabase/.temp

# OS and editors
.DS_Store
*.swp
*.swo
Thumbs.db

# Logs
*.log
npm-debug.log*
