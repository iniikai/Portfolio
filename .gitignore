# macOS clutter
.DS_Store
._*

# Bluehost / cPanel server artifacts — not part of the site
.ftpquota
error_log

# Node dependency trees — regenerable via npm/yarn install, never committed.
node_modules/

# Real secrets — never commit. Only *.example/*.sample env templates are tracked.
.env
.env.local
.env.*.local
website_4147d67d/admin/.env.local

# Large binaries (video, design files, oversized PDFs) are tracked via
# Git LFS instead of being excluded — see .gitattributes.
