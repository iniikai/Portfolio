# macOS clutter
.DS_Store
._*

# Bluehost / cPanel server artifacts — not part of the site
.ftpquota
error_log

# Node dependency trees — regenerable via npm/yarn install, never committed.
node_modules/

# Real secrets — never commit. Only *.example/*.sample env templates are tracked.
.env
.env.local
.env.*.local
website_4147d67d/admin/.env.local

# Files over GitHub's 100MB hard push limit. Git LFS was attempted but its
# batch-upload API is unreachable from this sandbox, so these 4 stay off
# GitHub and remain served from Bluehost until pushed via LFS from a machine
# with unrestricted GitHub access.
img/benjerrys/benjerrysinitial.pdf
img/birks/Birks Sequence Null_2.mp4
img/birks/videos/BirksSlides.m4v
port files/graphiics.sketch
