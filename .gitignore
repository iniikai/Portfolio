# macOS clutter
.DS_Store
._*

# Bluehost / cPanel server artifacts — not part of the site
.ftpquota
cgi-bin/
error_log

# Large video files stay on the Bluehost server and are served from there.
# They exceed GitHub's practical file-size limits and don't belong in git.
# Deployment must be configured NOT to delete untracked files, or these
# will be removed from the live site.
*.mp4
*.mov
*.webm
